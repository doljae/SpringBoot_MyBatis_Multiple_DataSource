<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>
	<!-- 이젠 이게 필요없음, beans.xml에서 설정해줬음 -->
	<!-- db 접속정보를 가진 properties file 정보 설정 -->
	<!-- <properties resource="db.properties"></properties> -->

	<!-- VO 객체 설정 -->
	<typeAliases>
		<typeAlias alias="User" type="myspring.user.vo.UserVO" />
	</typeAliases>
	<!-- 이젠 이게 필요없음, beans.xml에서 DataSource bean을 등록해주었기 때문 -->
	<!-- DataSource 설정 -->
	<!-- <environments default="development"> -->
	<!-- <environment id="development"> -->
	<!-- <transactionManager type="JDBC" /> -->
	<!-- 원래는 여기 HikariCP 가 적히는 곳임 -->
	<!-- <dataSource type="POOLED"> -->
	<!-- 런타임시에 driver가 불러온 xml 파일의 db.driver에 할당된 값이 요기 치환되어서 들어감 -->
	<!-- <property name="driver" value="${db.driver}"/> -->
	<!-- <property name="url" value="${db.url}"/> -->
	<!-- <property name="username" value="${db.username}"/> -->
	<!-- <property name="password" value="${db.password}"/> -->
	<!-- </dataSource> -->
	<!-- </environment> -->
	<!-- </environments> -->
	<!-- SQL을 포함하고 있는 Mapper.xml을 설정해줌 -->
	<!-- <mappers> -->
	<!-- <mapper resource="UserMapper.xml"/> -->
	<!-- </mappers> -->

</configuration>
